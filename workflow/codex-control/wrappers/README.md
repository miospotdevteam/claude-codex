# Wrappers

This directory is the future landing zone for Codex-specific wrappers and
control scripts once they are migrated out of the current transitional setup.

Wrappers should enforce or expose shared-core behavior, not redefine the
underlying contracts locally.
