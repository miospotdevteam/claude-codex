# Codex Skill Pack

This directory is the future landing zone for the Codex-native skill pack after
it is reshaped out of the staged upstream copy.

Expected source:

- `workflow/codex-control/plugin/source/codex-skills/`

The skill pack should consume shared contracts from `workflow/core/` where
appropriate, but remain Codex-native in runtime behavior.

See `shared-contract-adapter.md` for the concrete mapping.

Canonical editable source:

- `workflow/codex-control/plugin/source/codex-skills/`

The temporary `source/` shadow copy has been removed. Edit the staged
`plugin/source/` tree directly.
