# Skills

This directory is the future landing zone for Codex-specific skills or prompt
surfaces that consume the shared ownership contract.

The Codex skill pack should reference shared-core planning/discovery contracts
instead of carrying its own divergent copies where semantics are meant to stay
aligned.
