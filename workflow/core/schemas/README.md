# Schemas

Future shared schemas and machine-readable contracts should live here.

Examples:

- routing-matrix JSON or YAML representations
- plan/discovery schema definitions
- validation schemas for orchestration examples

Current canonical schema docs:

- `plan-schema.md`
- `master-plan-format.md`
