# Claude Manifests

This directory is the future landing zone for Claude-specific plugin manifest
files and related runtime package metadata.

Expected source:

- `workflow/claude-control/plugin/source/look-before-you-leap/.claude-plugin/`
- `workflow/claude-control/plugin/source/.claude-plugin/`

Canonical editable source:

- `workflow/claude-control/plugin/source/look-before-you-leap/.claude-plugin/`
- `workflow/claude-control/plugin/source/.claude-plugin/`

The temporary `source/` shadow copy has been removed. Edit the staged
`plugin/source/` tree directly.
