# Claude Hooks

This directory is the future landing zone for Claude-specific hook runtime
files once they are reshaped out of the staged upstream copy.

Expected source:

- `workflow/claude-control/plugin/source/look-before-you-leap/hooks/`

This remains Claude-only runtime behavior and should not move into
`workflow/core/`.

Canonical editable source:

- `workflow/claude-control/plugin/source/look-before-you-leap/hooks/`

The temporary `source/` shadow copy has been removed. Edit the staged
`plugin/source/` tree directly.
