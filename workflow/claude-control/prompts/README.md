# Prompts

This directory is the future landing zone for Claude-specific orchestration
prompt surfaces that consume the shared ownership contract.

These prompts should bind shared-core placeholders to Claude surfaces rather
than duplicating contract text locally.
